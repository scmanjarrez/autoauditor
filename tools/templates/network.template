{
    "client": {
        "description": "wallet where user ID are stored",
        "id": "User ID",
        "wallet": {
            "description": "File system wallet where user ID is stored",
            "path": "Path to wallet"
        },
        "credentials": {
            "description": "optionally, a credential path can be used if user ID can't be loaded from wallet",
            "cert": "Path to user certificate.",
            "private_key": "Path to user private key."
        }
    },
    "network": {
        "description": "test-network, 1 peer needed to use discovery service, 1 client to operate and 1 channel where peer is a member",
        "name": "test-network",
        "channel": "Channel name",
        "organization": {
            "description": "Information related to the organization user belongs to",
            "name": "Organization name",
            "mspid": "Organization MSP ID",
            "peer": {
                "grpc_request_endpoint": "Peer url",
                "server_hostname": "Peer hostname",
                "tls_cacerts": "Path to organization CA Cert"
            }
        }
    }
}

{
    "client": {
        "description": "wallet where user ID is stored",
        "id": "User1",
        "wallet": {
            "description": "File system wallet where user ID is stored",
            "path": "wallet-test"
        },
        "credentials": {
            "description": "optionally, a credential path can be used if user ID can't be loaded from wallet",
            "cert": "hlfabric/organizations/peerOrganizations/allsafe.com/users/User1@allsafe.com/msp/signcerts/User1@allsafe.com-cert.pem",
            "private_key": "hlfabric/organizations/peerOrganizations/allsafe.com/users/User1@allsafe.com/msp/keystore/priv_sk"
        }
    },
    "network": {
        "description": "groupsig-network, 1 anchor peer (discovery), 1 channel",
        "name": "groupsig-network",
        "channel": "evidences",
        "organization": {
            "description": "organization information where user ID belongs to",
            "name": "Allsafe",
            "mspid": "allsafeMSP",
            "peer": {
                "grpc_request_endpoint": "peer0.allsafe.com:8051",
                "server_hostname": "peer0.allsafe.com",
                "tls_cacerts": "hlfabric/organizations/peerOrganizations/allsafe.com/msp/tlscacerts/tlsca.allsafe.com-cert.pem"
            }
        }
    }
}
